<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zzclearning.gulimall.product.dao.SkuSaleAttrValueDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.zzclearning.gulimall.product.entity.SkuSaleAttrValueEntity" id="skuSaleAttrValueMap">
        <result property="id" column="id"/>
        <result property="skuId" column="sku_id"/>
        <result property="attrId" column="attr_id"/>
        <result property="attrName" column="attr_name"/>
        <result property="attrValue" column="attr_value"/>
        <result property="attrSort" column="attr_sort"/>
    </resultMap>
    <resultMap id="skuItemSaleAttrs" type="com.zzclearning.gulimall.product.vo.SkuItemVo$SaleAttrVo">
        <result property="attrId" column="attr_id"/>
        <result property="attrName" column="attr_name"/>
        <collection property="attrValues" ofType="com.zzclearning.gulimall.product.vo.SkuItemVo$AttrWithSkusVo">
            <result property="attrValue" column="attr_value"/>
            <result property="skuIds" column="skuids"/>
        </collection>
    </resultMap>
    <select id="getSkuItemSaleAttrs" resultMap="skuItemSaleAttrs">
        SELECT attr_id,attr_name,ssa.`attr_value`,GROUP_CONCAT(DISTINCT ssa.`sku_id`) skuids
        FROM pms_sku_sale_attr_value ssa
        LEFT JOIN pms_sku_info si ON ssa.`sku_id` = si.`sku_id`
        WHERE si.spu_id = #{spuId}
        GROUP BY attr_id,attr_name,attr_value
    </select>
    <select id="getSkuAttrvalues" resultType="java.lang.String">
        SELECT CONCAT(attr_name,"：",attr_value) FROM pms_sku_sale_attr_value WHERE sku_id = #{skuId}
    </select>


</mapper>